<?xml version="1.0" encoding="UTF-8"?>
<odoo>
 
<!-- /filtro pesquisa -->
    <record id="view_employee_filter" model="ir.ui.view">
        <field name="name">important.link.search</field>
        <field name="model">important.link</field>
        <field name="arch" type="xml">
            <search string="Links">
                <field name="nome" string="Nome" filter_domain="['|', ('nome', 'ilike', self), ('nome', 'ilike', self)]"/>
                <searchpanel>
                    <field name="category_id"  icon="fa fa-list" enable_counters="1"/>
        
                </searchpanel>
                <field name="nome"/>
                <separator/>
                
                <!-- 
                <filter name="my_team" string="My Team" domain="[('parent_id.user_id', '=', uid)]"/>
                <filter name="my_department" string="My Department" domain="[('member_of_department', '=', True)]"/>
                <separator/>
                <filter name="newly_hired" string="Newly Hired" domain="[('newly_hired', '=', True)]"/>
                <separator/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter name="group_manager" string="Manager" domain="[]" context="{'group_by': 'parent_id'}"/>
                    <filter name="group_department" string="Department" domain="[]" context="{'group_by': 'department_id'}"/>
                    <filter name="group_job" string="Job" domain="[]" context="{'group_by': 'job_id'}"/>
                    <filter name="group_start" string="Start Date" domain="[]" context="{'group_by': 'create_date'}"/>
                    <filter name="group_category_ids" string="Tags" domain="[]" context="{'group_by': 'category_ids'}"/>
                </group>
                -->
                
            </search>
        </field>
    </record>
<!-- vista kanban para ver  os links-->
<record id="kanban_view_important_link" model="ir.ui.view">
    <field name="name">important.link.kanban</field>
    <field name="model">important.link</field>
    <field name="priority">10</field>
    <field name="arch" type="xml">
        <kanban class="important_link_kanban" sample="1">
            <field name="id"/>
            <field name="nome"/>
            <field name="image_1920"/>
            <field name="url"/>
            <field name="descricao"/>
            <field name="category_id"/>
            
            <!-- Definindo o template do kanban -->
            <templates>
                <t t-name="kanban-box">
                    <div class="oe_kanban_global_click o_kanban_record_has_image_fill o_hr_kanban_record">
                        <!-- Verifica se há uma imagem presente -->
                        <t t-if="record.image_1920.raw_value">
                            <field name="image_1920" class="o_kanban_image_fill_left d-block" preview_image="image_1920"
                                widget="image" options="{'zoom': true, 'zoom_delay': 500}"/>
                        </t>
                        <!-- Caso contrário, exibe um ícone padrão -->
                        <t t-else="">
                            <div class="o_kanban_image_fill_left d-flex align-items-center justify-content-center bg-10 bg-gradient">
                                <svg class="w-75 h-75 opacity-50" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                                    <g fill="currentColor">
                                        <path d="M 10 11 C 4.08 11 2 14 2 16 L 2 19 L 18 19 L 18 16 C 18 14 15.92 11 10 11 Z"/>
                                        <circle cx="10" cy="5.5" r="4.5"/>
                                    </g>
                                </svg>
                            </div>
                        </t>
                        
                        <!-- Detalhes do kanban -->
                        <div class="oe_kanban_details">
                            <div class="o_kanban_record_top">
                                <div class="o_kanban_record_headings">
                                    <strong class="o_kanban_record_title">
                                        <field name="nome" placeholder="Nome do link importante"/> 
                                    </strong>
                                </div>
                            </div>
                            <ul>
                                <li>
                                    <field name="descricao" class="o_text_overflow"/>
                                </li>
                                <li>
                                    <field name="url" class="o_force_ltr"/>
                                </li>
                            </ul>
                        </div>
                    </div>
                </t>
            </templates>
        </kanban>
    </field>
</record>

    
    <!-- registro da chave estrangeira  -->
    <record id="view_link_category_form" model="ir.ui.view">
        <field name="name">link.category.form</field>
        <field name="model">link.category</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    
    
    <!-- Existing views: Tree and Form -->
    <record id="view_important_link_tree" model="ir.ui.view">
        <field name="name">important.link.tree</field>
        <field name="model">important.link</field>
        <field name="arch" type="xml">
            <tree string="Important Links">
                <field name="nome"/>
                <field name="url"/>
                <field name="category_id"  readonly="1" optional="show"/>
            </tree>
        </field>
    </record>

    <record id="view_important_link_form" model="ir.ui.view">
        <field name="name">important.link.form</field>
        <field name="model">important.link</field>
        <field name="arch" type="xml">
            <form string="Important Links">
                <sheet>
                    <group>
              
                        <field name="image_1920" widget='image' class="oe_avatar m-0" options='{"zoom": true, "preview_image":"image_1920"}'/>
                        <field name="nome"/>
                        <field name="url"/>
                        <field name="descricao"/>
                        <field name="category_id" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action to open the views: Kanban, Tree, and Form -->
    <record id="action_important_link" model="ir.actions.act_window">
        <field name="name">My links</field>
        <field name="res_model">important.link</field>
        <field name="view_mode">kanban,tree,form</field>
    </record>

    <!-- Menu items -->
    <menuitem id="menu_important_link" name="Devtrust Links" sequence="10"/>
    <menuitem id="submenu_important_link" name="Links" parent="menu_important_link" action="action_important_link"/>
</odoo>